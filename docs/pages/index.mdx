import { Tabs, Tab, Steps } from 'nextra-theme-docs';

import Link from 'next/link'
import CounterExample from 'common/Counter';

## Overview

Classes which extend `Model` can manage behavior for components. Models are easy to extend, use and even provide via context. While hooks are great, state and logic are not a part of well-formed JSX. Models help wrap that stuff in robust, typescript controllers instead.

## Getting Started

<Steps>
  ### Install `@expressive/react`

  <Tabs items={['npm', 'yarn', 'pnpm']} storageKey="selected-pkg-manager">
    <Tab>
      ```bash copy
      npm i @expressive/react
      ```
    </Tab>
    <Tab>
      ```bash copy
      yarn add @expressive/react
      ```
    </Tab>
    <Tab>
      ```bash copy
      pnpm install @expressive/react
      ```
    </Tab>
  </Tabs>

  ### Import and extend `Model`

  `Model` class is designed to be extended with whatever state and logic you choose.
  It facilitates a reactive, observable state container for your components but that it.
  The rest is up to you.

  ```js
    import Model from "@expressive/react";

    class Controller extends Model {
      current = 1

      increment = () => { this.current += 1 };
      decrement = () => { this.current -= 1 };
    }
  ```

  > Here we'll also define some methods. This isolates the logic for incrementing
  > and decrementing the counter, so it doesn't need to live in the component.

  ### Use `Controller` as a hook

  Here the `use()` method (inherited from `Model`) is hook which will create,
  memoize and subscribe to all state used by this component.

  ```js
    function Counter(){
      const { current, increment, decrement } = Controller.use();

      return (
        <div>
          <button onClick={decrement}>{"-"}</button>
          <pre>{current}</pre>
          <button onClick={increment}>{"+"}</button>
        </div>
      )
    }
  ```

  ### Play with it!

  <CounterExample />

  Done already? Apparently we have something usable. 🚀 <br />
  [View in codesandbox](https://codesandbox.io/s/example-counter-th8xl)

</Steps>