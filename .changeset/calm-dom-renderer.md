---
"@expressive/dom": minor
---

Add an MVC-native client DOM renderer with function and class components, context, native events, keyed reconciliation, direct reactive collections, portals, lazy components, error/suspense boundaries, and transition-aware committed-content retention.
