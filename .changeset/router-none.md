---
'@expressive/router': minor
---

Rename the scoped no-match `Route` prop from `default` to `none`. Replace
`<Route default ...>` with `<Route none ...>`; matching and force-404 behavior
are unchanged.
